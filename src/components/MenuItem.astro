---
import type { HTMLAttributes } from "astro/types";

export interface Props {
  url: string;
  title: string;
}

const { url, title } = Astro.props;
const currentPath = Astro.url.pathname;
---

<a href={url} class={currentPath.startsWith(url) ? "underline" : ""}>{title}</a>
